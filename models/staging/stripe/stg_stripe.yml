version: 2

models:
  - name: stg_stripe__payments
    description: Payments from stripe service
    columns:
      - name: payment_id
        description: Primary key for payments.
        data_tests:
          - unique
          - not_null
